import styled from '@emotion/styled'
import { Form } from 'antd'

export const FormWrapper = styled(Form)`
  margin-bottom: 2rem;
`

export const TabelWrapper = styled.div`
  /* .ant-table-wrapper {
    min-width: 500px;
  } */
  /* overflow: hidden; */
  overflow-y: auto;
`

// 可使用emotion行內樣式 不過不太喜歡
